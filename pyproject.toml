[project]
name = "statserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DV155", email = "dan.vasilkovskiy@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=2.1.1",
]

[project.scripts]
statserver = "statserver:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
